import |sqrt| from "math";
import |println| from "console";
import |newVec| from "./lib/vec.grib";

im v = newVec(3, 4, 5);

println("Before: " + v.len);
v.len = 14;
println("After: |<" + v.x + ", " + v.y + ", " + v.z + ">| = " + v.len);