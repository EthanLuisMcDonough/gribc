#include "operators.h"

struct GribValue add_gribvalues(struct GribValue one, struct GribValue two) {
    // @TODO
    
    if (one.type == STRING) {
        //
    } else {
        
    }
    
    
    return GRIBNIL;
}
